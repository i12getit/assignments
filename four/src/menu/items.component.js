//create a component called `items` that shows all of the menu items for a particular category.

//should *NOT* directly use the `MenuDataService` to fetch their own data.
//Instead, the proper data should be simply passed into the component. (Hint: use the one-way `<` binding).
(function() {
  'use strict';
  
}());
